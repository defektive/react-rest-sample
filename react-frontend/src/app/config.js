function config() {
  'use strict';
  var Config = {};
  // Begin config

  Config.APP_NAME = "I am an app name";

  // End config
  Object.freeze(Config);
  return Config;
}

export default config()
